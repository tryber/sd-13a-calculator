function subtract(a, b) {
  return a - b;
}

const soma = ( x, y) => {


  return x + y
}

function fatorial(num) {
  let total = 1;
  for (let i = 2; i <= num; i++){
    total = total * i;
  }
  return total;
}
